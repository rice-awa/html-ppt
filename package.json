{
  "name": "htmlppt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve public"
  },
  "devDependencies": {
    "playwright": "^1.45.0"
  }
}
